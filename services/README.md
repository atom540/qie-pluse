# Services Directory

This directory contains the core AI service implementations:

- `sentiment/`: NLP sentiment analysis engine
- `timeseries/`: Market data analysis engine  
- `inference/`: Risk scoring and blockchain relay
- `collectors/`: Data collection services

## Architecture

Each service is designed as an independent module with clear interfaces
for easy testing and deployment.
